[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mlb-data-platform"
version = "0.1.0"
description = "MLB Data Engineering Platform"
requires-python = ">=3.11"

dependencies = [
    "PyYAML"
]

[tool.setuptools.packages.find]
where = ["src"]